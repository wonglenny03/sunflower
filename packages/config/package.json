{
  "name": "@company-search/config",
  "version": "1.0.0",
  "description": "Shared configuration for Company Search System",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@company-search/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}

